<h1>Search Tracks:</h1>
<div>
  {{#each tracks}}
    <div>
      <p>{{title}} by {{artist_name}} | {{genre}}</p>
      <p>
        Length: {{length}}
        {{#if is_explicit}}
          | <bold>explicit</bold>
        {{/if}}
      </p>
    </div>
  {{/each}}
</div>